namespace OptimFoundation.Core
{
    public enum VarType
    {
        Continuous,
        Integer,
        Binary
    }

    public enum ConstraintSense
    {
        LessEqual,
        Equal,
        GreaterEqual
    }

    public enum ObjectiveSense
    {
        Minimize,
        Maximize
    }
}
